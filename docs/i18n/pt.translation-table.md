# pt Translation Table

Base locale: `zh`
Secondary locale: `en`
Target locale: `pt`

| Key | Status | Source | Secondary | Target |
| --- | --- | --- | --- | --- |
| `about.copyright` | translated | © 2025 hooosberg. 保留所有权利。 | © 2025 hooosberg. All rights reserved. | © 2025 hooosberg. Todos os direitos reservados. |
| `about.descFeatures` | same-as-secondary | 精确元素选择与 CSS 选择器 • 样式差异追踪（修改前/后）• 自然语言标签注释 • 即时可视反馈与撤销/重做 • 适配 Cursor、Claude Code、Codex、Copilot 等 | Precise element selection with CSS selectors • Style diff tracking (before/after) • Natural language tags • Instant visual feedback with undo/redo • Works with Cursor, Claude Code, Codex, Copilot and more | Precise element selection with CSS selectors • Style diff tracking (before/after) • Natural language tags • Instant visual feedback with undo/redo • Works with Cursor, Claude Code, Codex, Copilot and more |
| `about.descPrivacy` | same-as-secondary | 本地优先，完全隐私。所有检查与提示词生成均在你的 Mac 上本地完成。 | Local-first, complete privacy. All inspection and prompt generation happens locally on your Mac. | Local-first, complete privacy. All inspection and prompt generation happens locally on your Mac. |
| `about.description` | translated | 用可视化的方式告诉 AI 你要改什么。DOMPrompter 让你直接在页面上选中元素、微调 CSS 参数、添加文字注释，自动生成结构化的 AI 提示词。 | Show AI exactly what you want to change. DOMPrompter lets you select elements directly on the page, visually adjust CSS parameters, add text annotations, and auto-generate structured AI prompts. | Inspetor DOM visual e gerador de prompts IA. Selecione elementos, ajuste estilos visualmente, gere prompts IA precisos para Cursor, Claude Code, Codex e mais. |
| `about.github` | same-as-base | GitHub | GitHub | GitHub |
| `about.privacy` | translated | 隐私政策 | Privacy Policy | Política de privacidade |
| `about.support` | translated | 支持中心 | Support | Central de suporte |
| `about.terms` | translated | 服务条款 | Terms of Service | Termos de serviço |
| `about.version` | same-as-secondary | 版本 {{version}} | Version {{version}} | Version {{version}} |
| `about.website` | translated | 官网 | Website | Site |
| `app.name` | same-as-base | DOMPrompter | DOMPrompter | DOMPrompter |
| `app.tagline` | same-as-secondary | 可视化微调 UI，为 Vibe Coding 生成精准提示词 | Visually tune your UI, generate precise prompts for Vibe Coding | Visually tune your UI, generate precise prompts for Vibe Coding |
| `common.collapse` | same-as-secondary | 收起 | Collapse | Collapse |
| `common.copied` | same-as-secondary | 已复制 | Copied | Copied |
| `common.copy` | same-as-secondary | 复制 | Copy | Copy |
| `common.expand` | same-as-secondary | 展开 | Expand | Expand |
| `common.tip` | same-as-secondary | 提示 | Tip | Tip |
| `license.benefitDiff` | same-as-secondary | 样式差异追踪（修改前/后） | Style diff tracking (before/after) | Style diff tracking (before/after) |
| `license.benefitExport` | same-as-secondary | 无限页面提示词导出 | Unlimited page prompt export | Unlimited page prompt export |
| `license.benefitFuture` | same-as-secondary | 包含所有未来 Pro 功能 | All future Pro features included | All future Pro features included |
| `license.benefitTags` | same-as-secondary | 自然语言标签与注释 | Natural language tags & annotations | Natural language tags & annotations |
| `license.ctaNote` | same-as-secondary | 一次付费，无订阅。Pro 功能永久解锁。 | One-time payment. No subscription. Pro features stay unlocked permanently. | One-time payment. No subscription. Pro features stay unlocked permanently. |
| `license.processing` | same-as-secondary | 处理中... | Processing... | Processing... |
| `license.promoDesc` | same-as-secondary | 充分发挥可视化检查工作流的潜力。导出整页提示词、追踪每次样式变化、使用自然语言注释。 | Get the most out of your visual inspection workflow. Export full-page prompts, track every style change, and annotate with natural language. | Get the most out of your visual inspection workflow. Export full-page prompts, track every style change, and annotate with natural language. |
| `license.promoTitle` | same-as-secondary | 解锁 DOMPrompter Pro | Unlock DOMPrompter Pro | Unlock DOMPrompter Pro |
| `license.proThanks` | same-as-secondary | 感谢支持 DOMPrompter！所有 Pro 功能已解锁。一次购买即可享有我们未来添加的每项 Pro 功能。 | Thank you for supporting DOMPrompter! All Pro features are unlocked. Your one-time purchase covers every future Pro feature we add. | Thank you for supporting DOMPrompter! All Pro features are unlocked. Your one-time purchase covers every future Pro feature we add. |
| `license.purchase` | same-as-secondary | 购买 Pro | Purchase Pro | Purchase Pro |
| `license.restore` | same-as-secondary | 恢复购买 | Restore Purchase | Restore Purchase |
| `license.upgradeCta` | same-as-secondary | 升级到 Pro — $19.99 | Upgrade to Pro — $19.99 | Upgrade to Pro — $19.99 |
| `onboarding.aiPrompt` | same-as-secondary | 复制 AI 适配提示词 | Copy AI setup prompt | Copy AI setup prompt |
| `onboarding.aiPromptDesc` | same-as-secondary | 这份提示词只关注网页项目启动、统一地址，并要求 AI 自己完成启动验证。 | This prompt only focuses on starting the web app, unifying the URL, and requiring the assistant to verify it. | This prompt only focuses on starting the web app, unifying the URL, and requiring the assistant to verify it. |
| `onboarding.aiSetupPrompt` | same-as-secondary | 我正在使用 DOMPrompter 调试一个网页项目。请帮我把项目的启动脚本适配到 DOMPrompter 的默认调试地址，让我不需要再手动复制粘贴地址，直接点击加载即可开始调试。<br><br>请先判断我的项目属于哪种网页项目（React / Vue / Svelte / Next.js / 纯 HTML / 其他前端项目），然后只围绕“网页能在浏览器里启动”这件事修改。优先只改 package.json 里的 scripts，不要改业务逻辑代码。<br><br>目标：<br>- 最终页面可以直接通过 {{defaultUrl}} 打开<br>- DOMPrompter 第 3 步默认地址可以直接加载成功<br>- 你修改完以后要自己运行并验证成功<br><br>请按下面要求处理：<br><br>1. 脚本适配<br>- 确保存在 dev 脚本<br>- 开发服务器必须监听 localhost<br>- 优先统一到 {{defaultUrl}}<br>- 如果是 Vite，可使用 vite --host localhost --port 5173<br>- 如果是 Next.js，可使用 next dev -H localhost -p 5173<br>- 如果是纯 HTML 且没有 package.json，请创建 package.json，并添加 "dev": "npx serve . -l 5173"<br><br>2. 修改限制<br>- 不要修改业务逻辑代码<br>- 保留项目原有其他 scripts<br>- 如果缺少 serve 或其他必需依赖，请直接安装<br>- 只关注网页调试，不需要处理 Electron、桌面端或 remote debugging 端口<br><br>3. 验证步骤必须执行<br>- 运行 npm run dev（或项目对应包管理器命令）<br>- 确认终端输出里有 localhost 地址<br>- 优先确认最终地址就是 {{defaultUrl}}<br>- 确认启动无报错后再停止进程<br><br>4. 最终输出必须包含<br>- 你识别出的网页项目类型<br>- 你修改了哪些 scripts<br>- 最终可直接打开的地址<br>- 你的验证结果<br>- 如果不能固定为 {{defaultUrl}}，明确说明原因，并给出实际可用地址<br><br>请直接开始分析并修改。只有你自己验证启动成功后，才算完成。 | I am using DOMPrompter to debug a web project. Please adapt the project's startup scripts to DOMPrompter's default debugging URL so I can click Load directly without manually copying and pasting a URL.<br><br>First identify what kind of web project this is (React / Vue / Svelte / Next.js / static HTML / other frontend project), then only focus on one goal: make the webpage start in the browser correctly. Prefer changing only package.json scripts and do not modify business logic.<br><br>Goal:<br>- The page should open directly at {{defaultUrl}}<br>- The default URL in step 3 of DOMPrompter should load successfully<br>- After making changes, you must run the project and verify it yourself<br><br>Requirements:<br><br>1. Script adaptation<br>- Ensure a dev script exists<br>- The dev server must listen on localhost<br>- Prefer unifying the final URL to {{defaultUrl}}<br>- For Vite, vite --host localhost --port 5173 is acceptable<br>- For Next.js, next dev -H localhost -p 5173 is acceptable<br>- For pure HTML without package.json, create one and add "dev": "npx serve . -l 5173"<br><br>2. Constraints<br>- Do not modify business logic<br>- Preserve all existing scripts<br>- If serve or any other required dependency is missing, install it<br>- Only focus on web debugging. Do not handle Electron, desktop workflows, or remote debugging ports<br><br>3. Required verification<br>- Run npm run dev (or the equivalent package manager command)<br>- Confirm the terminal prints a localhost URL<br>- Prefer confirming that the final URL is exactly {{defaultUrl}}<br>- Stop the process only after verifying it starts without errors<br><br>4. Final output must include<br>- The detected web project type<br>- The scripts you changed<br>- The final URL that can be opened directly<br>- Your verification result<br>- If {{defaultUrl}} is not possible, explain why and provide the exact working fallback URL<br><br>Start the analysis and make the changes now. The task is only complete after you verify that the web app starts successfully. | I am using DOMPrompter to debug a web project. Please adapt the project's startup scripts to DOMPrompter's default debugging URL so I can click Load directly without manually copying and pasting a URL.<br><br>First identify what kind of web project this is (React / Vue / Svelte / Next.js / static HTML / other frontend project), then only focus on one goal: make the webpage start in the browser correctly. Prefer changing only package.json scripts and do not modify business logic.<br><br>Goal:<br>- The page should open directly at {{defaultUrl}}<br>- The default URL in step 3 of DOMPrompter should load successfully<br>- After making changes, you must run the project and verify it yourself<br><br>Requirements:<br><br>1. Script adaptation<br>- Ensure a dev script exists<br>- The dev server must listen on localhost<br>- Prefer unifying the final URL to {{defaultUrl}}<br>- For Vite, vite --host localhost --port 5173 is acceptable<br>- For Next.js, next dev -H localhost -p 5173 is acceptable<br>- For pure HTML without package.json, create one and add "dev": "npx serve . -l 5173"<br><br>2. Constraints<br>- Do not modify business logic<br>- Preserve all existing scripts<br>- If serve or any other required dependency is missing, install it<br>- Only focus on web debugging. Do not handle Electron, desktop workflows, or remote debugging ports<br><br>3. Required verification<br>- Run npm run dev (or the equivalent package manager command)<br>- Confirm the terminal prints a localhost URL<br>- Prefer confirming that the final URL is exactly {{defaultUrl}}<br>- Stop the process only after verifying it starts without errors<br><br>4. Final output must include<br>- The detected web project type<br>- The scripts you changed<br>- The final URL that can be opened directly<br>- Your verification result<br>- If {{defaultUrl}} is not possible, explain why and provide the exact working fallback URL<br><br>Start the analysis and make the changes now. The task is only complete after you verify that the web app starts successfully. |
| `onboarding.chooseDesc` | same-as-secondary | 通过本地开发服务器或直接打开静态 HTML 文件开始调试。 | Use a local dev server or open a static HTML file directly. | Use a local dev server or open a static HTML file directly. |
| `onboarding.chooseTitle` | same-as-secondary | 选择开始方式 | Choose how you want to start | Choose how you want to start |
| `onboarding.copied` | same-as-secondary | 已复制 | Copied | Copied |
| `onboarding.copyCommand` | same-as-secondary | 复制命令 | Copy Command | Copy Command |
| `onboarding.copyPrompt` | same-as-secondary | 复制提示词 | Copy Prompt | Copy Prompt |
| `onboarding.exit` | same-as-secondary | 退出 | Exit | Exit |
| `onboarding.htmlDesc` | same-as-secondary | 直接打开本地 HTML 文件并开始检查 | Open a local HTML file and inspect it immediately | Open a local HTML file and inspect it immediately |
| `onboarding.htmlFoot` | same-as-secondary | 选择文件即可开始 | Select file to start | Select file to start |
| `onboarding.htmlGuide` | same-as-secondary | 适合单文件页面、静态导出页和本地原型。直接打开文件，或从最近记录继续。 | Best for single-file pages, static exports, and local prototypes. Open a file directly or continue from recent history. | Best for single-file pages, static exports, and local prototypes. Open a file directly or continue from recent history. |
| `onboarding.htmlGuideBody` | same-as-secondary | 不需要先跑本地服务，选中一个 HTML 文件后会立即加载到画布里，适合快速检查和局部微调。 | You do not need to start a dev server first. Pick an HTML file and DOMPrompter will load it straight into the canvas for inspection. | You do not need to start a dev server first. Pick an HTML file and DOMPrompter will load it straight into the canvas for inspection. |
| `onboarding.htmlGuideTitle` | same-as-secondary | 直接打开本地 HTML | Open Local HTML Directly | Open Local HTML Directly |
| `onboarding.htmlHistory` | same-as-secondary | 最近记录 | Recent History | Recent History |
| `onboarding.htmlHistoryEmpty` | same-as-secondary | 还没有最近打开的 HTML 文件。 | No HTML files have been opened recently. | No HTML files have been opened recently. |
| `onboarding.htmlMode` | same-as-secondary | HTML 模式 | HTML Mode | HTML Mode |
| `onboarding.htmlOpenAction` | same-as-secondary | 打开 HTML 文件 | Open HTML File | Open HTML File |
| `onboarding.htmlRecentAction` | same-as-secondary | 重新打开 | Reopen | Reopen |
| `onboarding.htmlTitle` | same-as-secondary | HTML 模式 | HTML Mode | HTML Mode |
| `onboarding.loadButton` | same-as-secondary | 加载页面 | Load Page | Load Page |
| `onboarding.loadPage` | same-as-secondary | 加载页面 | Load your page | Load your page |
| `onboarding.loadPageDesc` | same-as-secondary | 如果 AI 已按规范适配完成，这里默认地址可以直接使用；如需改端口，直接修改顶部地址栏即可。 | If the script was adapted correctly, the default address below should work immediately. If not, edit the address in the top bar. | If the script was adapted correctly, the default address below should work immediately. If not, edit the address in the top bar. |
| `onboarding.loadPreset` | same-as-secondary | 加载默认地址 | Load Default Address | Load Default Address |
| `onboarding.next` | same-as-secondary | 下一步 | Next | Next |
| `onboarding.philosophy.body` | same-as-secondary | DOMPrompter 是一个介于 AI 和专业代码之间的可视化辅助工具。它不直接改源码，而是帮助你标记对象、记录参数变化，并生成可交付给 AI 的结构化提示词。 | DOMPrompter is a visual assistant between AI and production code. It does not rewrite source files itself. It helps you mark targets, record parameter changes, and assemble structured prompts that AI can apply correctly. | DOMPrompter is a visual assistant between AI and production code. It does not rewrite source files itself. It helps you mark targets, record parameter changes, and assemble structured prompts that AI can apply correctly. |
| `onboarding.philosophy.eyebrow` | same-as-secondary | AI 与代码之间 | Between AI And Code | Between AI And Code |
| `onboarding.philosophy.flow.generate` | same-as-secondary | 生成 | Generate | Generate |
| `onboarding.philosophy.flow.handToAi` | same-as-secondary | 交给 AI | Hand To AI | Hand To AI |
| `onboarding.philosophy.flow.mark` | same-as-secondary | 标记 | Mark | Mark |
| `onboarding.philosophy.flow.tune` | same-as-secondary | 微调 | Tune | Tune |
| `onboarding.philosophy.footerBody` | same-as-secondary | 页面内 Overlay 负责即时反馈，属性工作台负责持久记录。这让整个调试过程保持轻、快、可信。 | The page overlay is responsible for immediate feedback. The workbench is responsible for durable state. That keeps the workflow light, fast, and trustworthy. | The page overlay is responsible for immediate feedback. The workbench is responsible for durable state. That keeps the workflow light, fast, and trustworthy. |
| `onboarding.philosophy.footerLead` | same-as-secondary | 精准选择，精确描述变更，再交给 AI 落地。 | Select precisely, describe precisely, then hand it to AI. | Select precisely, describe precisely, then hand it to AI. |
| `onboarding.philosophy.htmlBadge` | same-as-secondary | 静态页面 | Static Page | Static Page |
| `onboarding.philosophy.modeHint` | same-as-secondary | 选择一种开始方式 | Choose how you want to begin | Choose how you want to begin |
| `onboarding.philosophy.serverBadge` | same-as-secondary | 默认推荐 | Recommended | Recommended |
| `onboarding.philosophy.title` | same-as-secondary | 把页面微调，变成 AI 能准确执行的说明 | Turn visual tweaks into instructions AI can execute precisely | Turn visual tweaks into instructions AI can execute precisely |
| `onboarding.previous` | same-as-secondary | 上一步 | Previous | Previous |
| `onboarding.serverDesc` | same-as-secondary | 你的项目已经运行在 localhost 上 | Your project already runs on localhost | Your project already runs on localhost |
| `onboarding.serverFoot` | same-as-secondary | 3 步引导配置 | 3-step guided setup | 3-step guided setup |
| `onboarding.serverGuide` | same-as-secondary | 先把网页项目启动到默认调试地址，再在上方地址栏一键加载。 | Start your web project on the default debugging URL, then load it from the address bar above. | Start your web project on the default debugging URL, then load it from the address bar above. |
| `onboarding.serverMode` | same-as-secondary | 服务模式配置流程 | Server Mode | Server Mode |
| `onboarding.serverTitle` | same-as-secondary | 服务模式 | Server Mode | Server Mode |
| `onboarding.startServer` | same-as-secondary | 启动开发服务器 | Start your dev server | Start your dev server |
| `onboarding.startServerDesc` | same-as-secondary | 在项目目录中运行 npm run dev，确认终端最终输出的是默认调试地址。 | Run npm run dev in your project directory and confirm the final localhost output matches the default debugging URL. | Run npm run dev in your project directory and confirm the final localhost output matches the default debugging URL. |
| `onboarding.topBarHint` | same-as-secondary | 不需要再在向导里手动输入地址。顶部地址栏可以继续刷新、切换地址并重新加载。 | You no longer need to type the address inside the wizard. Use the top address bar for refresh, quick edits, and reloads. | You no longer need to type the address inside the wizard. Use the top address bar for refresh, quick edits, and reloads. |
| `panel.ready` | same-as-secondary | 准备开始检查 | Ready to inspect | Ready to inspect |
| `panel.readyDesc` | same-as-secondary | 在页面中悬停并点击元素，右侧工作台会显示对应样式。 | Hover and click an element in the page to bring its styles into the workbench. | Hover and click an element in the page to bring its styles into the workbench. |
| `paywall.benefitExportDesc` | translated | 导出完整页面 AI 提示词，融合元素选择器、样式差异和标签注释为一体。 | Export full-page AI prompts combining element selectors, style diffs, and annotations into one structured output. | Exporte prompts combinando seletores, diferenças de estilo e anotações em uma saída estruturada. |
| `paywall.benefitExportTitle` | translated | 无限页面提示词导出 | Unlimited page prompt export | Exportação ilimitada de prompts |
| `paywall.benefitFutureDesc` | translated | 一次购买，永久享有每项新增 Pro 功能。无订阅费用。 | One-time purchase covers every Pro feature we add, forever. No subscriptions. | Compra única para todas as funções Pro atuais e futuras. Sem assinatura. |
| `paywall.benefitFutureTitle` | translated | 包含所有未来 Pro 功能 | All future Pro features | Todas as futuras funções Pro |
| `paywall.benefitPrecisionDesc` | translated | 每次 CSS 微调自动记录前后对比 — width: 200px → 300px，AI 一目了然。 | Every CSS tweak auto-recorded as before/after — width: 200px → 300px. Crystal clear for AI to execute. | Cada ajuste CSS registrado automaticamente antes/depois — width: 200px → 300px. Claro para a IA. |
| `paywall.benefitPrecisionTitle` | translated | 样式差异追踪 | Style diff tracking | Rastreamento de diferenças de estilo |
| `paywall.benefitTagsDesc` | translated | 为元素添加意图注释：「按钮颜色太深」「间距太大」。让 AI 理解你的设计思路。 | Annotate elements with intent: "button color too dark", "spacing too wide". AI understands your design thinking. | Anote elementos com intenção: “cor do botão muito escura”, “espaçamento muito grande”. A IA entende seu design. |
| `paywall.benefitTagsTitle` | translated | 自然语言标签 | Natural language tags | Tags em linguagem natural |
| `paywall.cancel` | translated | 取消 | Cancel | Cancelar |
| `paywall.ctaNote` | translated | 一次付费，无订阅费用。Pro 功能永久解锁。 | One-time payment. No subscription fees. Pro features stay unlocked permanently. | Pagamento único. Sem taxas de assinatura. As funções Pro permanecem desbloqueadas permanentemente. |
| `paywall.kicker` | same-as-base | DOMPROMPTER PRO | DOMPROMPTER PRO | DOMPROMPTER PRO |
| `paywall.lifetimeBadge` | translated | 终身买断 | Lifetime unlock | Licença vitalícia |
| `paywall.priceValue` | same-as-base | $19.99 | $19.99 | $19.99 |
| `paywall.purchaseCancelled` | translated | 购买已取消。 | Purchase was cancelled. | A compra foi cancelada. |
| `paywall.purchaseFailed` | translated | 购买失败，请重试。 | Purchase failed. Please try again. | A compra falhou. Tente novamente. |
| `paywall.purchasing` | translated | 正在购买... | Purchasing... | Comprando... |
| `paywall.restoreFailed` | translated | 恢复失败，请重试。 | Restore failed. Please try again. | A restauração falhou. Tente novamente. |
| `paywall.restoreNotFound` | translated | 未找到之前的购买记录。 | No previous purchase found. | Nenhuma compra anterior encontrada. |
| `paywall.restorePurchase` | translated | 恢复购买 | Restore Purchase | Restaurar compra |
| `paywall.restoring` | translated | 正在恢复... | Restoring... | Restaurando... |
| `paywall.subtitle` | translated | 为每个检查的页面导出精确的 AI 提示词。一次购买，无限精准。 | Export precise AI prompts for every page you inspect. One purchase, unlimited precision. | Exporte prompts IA precisos para cada página inspecionada. Uma compra, precisão ilimitada. |
| `paywall.title` | translated | 解锁全部能力 | Unlock Full Power | Desbloqueie todo o poder |
| `paywall.unlockNow` | translated | 升级到 Pro | Upgrade to Pro | Atualizar para Pro |
| `properties.fields.alignItems.helperText` | same-as-secondary | 控制子元素沿交叉轴的对齐方式。 | Control how children align along the cross axis. | Control how children align along the cross axis. |
| `properties.fields.alignItems.label` | same-as-secondary | 交叉轴 | Cross Axis | Cross Axis |
| `properties.fields.backgroundColor.helperText` | same-as-secondary | 直接调整背景综合色彩，快速推敲层级与强调。 | Adjust the background color to explore hierarchy and emphasis. | Adjust the background color to explore hierarchy and emphasis. |
| `properties.fields.backgroundColor.label` | same-as-secondary | 背景色 | Background | Background |
| `properties.fields.border.helperText` | same-as-secondary | 控制线条粗细、颜色和样式。 | Control thickness, color, and line style for cards, inputs, and buttons. | Control thickness, color, and line style for cards, inputs, and buttons. |
| `properties.fields.border.label` | same-as-secondary | 边框 | Border | Border |
| `properties.fields.border.placeholder` | same-as-base | 1px solid rgba(255,255,255,.12) | 1px solid rgba(255,255,255,.12) | 1px solid rgba(255,255,255,.12) |
| `properties.fields.borderRadius.helperText` | same-as-secondary | 调整卡片、面板和按钮的圆角大小。 | Adjust corner radius for cards, panels, and buttons. | Adjust corner radius for cards, panels, and buttons. |
| `properties.fields.borderRadius.label` | same-as-secondary | 圆角 | Radius | Radius |
| `properties.fields.boxShadow.helperText` | same-as-secondary | 用阴影塑造层级和立体感。 | Use shadow to create elevation and depth. | Use shadow to create elevation and depth. |
| `properties.fields.boxShadow.label` | same-as-secondary | 投影 | Shadow | Shadow |
| `properties.fields.boxShadow.placeholder` | same-as-base | 0 10px 30px rgba(0,0,0,.18) | 0 10px 30px rgba(0,0,0,.18) | 0 10px 30px rgba(0,0,0,.18) |
| `properties.fields.color.helperText` | same-as-secondary | 调整文字颜色以表达层级、品牌色和弱化信息。 | Adjust the text color for hierarchy, branding, and secondary copy. | Adjust the text color for hierarchy, branding, and secondary copy. |
| `properties.fields.color.label` | same-as-secondary | 文字颜色 | Text Color | Text Color |
| `properties.fields.display.helperText` | same-as-secondary | 切换容器布局模式。只有在 Flex 或 Grid 下，对齐与 Gap 才真正生效。 | Switch the container's layout mode. Alignment and gap only truly work in Flex or Grid. | Switch the container's layout mode. Alignment and gap only truly work in Flex or Grid. |
| `properties.fields.display.label` | same-as-secondary | 显示方式 | Display | Display |
| `properties.fields.fontFamily.helperText` | same-as-secondary | 切换字体族，快速比较文案气质。 | Swap the font family to compare tone quickly. | Swap the font family to compare tone quickly. |
| `properties.fields.fontFamily.label` | same-as-secondary | 字体 | Font Family | Font Family |
| `properties.fields.fontFamily.placeholder` | same-as-base | Georgia, serif | Georgia, serif | Georgia, serif |
| `properties.fields.fontSize.helperText` | same-as-secondary | 调整标题、副标题和正文的文字尺寸。 | Adjust the text size for titles, subtitles, and body copy. | Adjust the text size for titles, subtitles, and body copy. |
| `properties.fields.fontSize.label` | same-as-secondary | 字号 | Font Size | Font Size |
| `properties.fields.fontWeight.helperText` | same-as-secondary | 改变文字轻重感，用于强调或弱化。 | Change the text weight to emphasize or soften copy. | Change the text weight to emphasize or soften copy. |
| `properties.fields.fontWeight.label` | same-as-secondary | 字重 | Weight | Weight |
| `properties.fields.fontWeight.placeholder` | same-as-base | 400 / 600 | 400 / 600 | 400 / 600 |
| `properties.fields.gap.helperText` | same-as-secondary | 调整子元素之间的空隙，仅在 Flex 或 Grid 下生效。 | Adjust the spacing between child elements. Only works in Flex or Grid. | Adjust the spacing between child elements. Only works in Flex or Grid. |
| `properties.fields.gap.label` | same-as-secondary | 间距 | Gap | Gap |
| `properties.fields.gap.placeholder` | same-as-base | 16px | 16px | 16px |
| `properties.fields.height.helperText` | same-as-secondary | 调整容器的最终高度，适合标题区、卡片和媒体框。 | Adjust the final height of the container. Great for title areas, cards, and media frames. | Adjust the final height of the container. Great for title areas, cards, and media frames. |
| `properties.fields.height.label` | same-as-secondary | 高度 | Height | Height |
| `properties.fields.height.placeholder` | same-as-base | 200px / auto | 200px / auto | 200px / auto |
| `properties.fields.imageBorderRadius.helperText` | same-as-secondary | 让封面图和头像等图片边缘更柔和。 | Soften the image corners for covers and avatars. | Soften the image corners for covers and avatars. |
| `properties.fields.imageHeight.helperText` | same-as-secondary | 调整图片或媒体框的最终高度。 | Adjust the final height of the image or media frame. | Adjust the final height of the image or media frame. |
| `properties.fields.imageHeight.placeholder` | same-as-base | 240px / auto | 240px / auto | 240px / auto |
| `properties.fields.imageWidth.helperText` | same-as-secondary | 调整图片或媒体框的最终宽度。 | Adjust the final width of the image or media frame. | Adjust the final width of the image or media frame. |
| `properties.fields.imageWidth.placeholder` | same-as-base | 320px / auto | 320px / auto | 320px / auto |
| `properties.fields.justifyContent.helperText` | same-as-secondary | 控制子元素沿主轴的分布方式。 | Control how children are distributed along the main axis. | Control how children are distributed along the main axis. |
| `properties.fields.justifyContent.label` | same-as-secondary | 主轴对齐 | Main Axis | Main Axis |
| `properties.fields.left.helperText` | same-as-secondary | 在定位元素上微调左侧偏移。 | Fine-tune the left offset on a positioned element. | Fine-tune the left offset on a positioned element. |
| `properties.fields.left.label` | same-as-secondary | 左侧偏移 | Left Offset | Left Offset |
| `properties.fields.left.placeholder` | same-as-base | 12px | 12px | 12px |
| `properties.fields.lineHeight.helperText` | same-as-secondary | 调整文本行间距，改善节奏和可读性。 | Adjust vertical spacing between lines for rhythm and readability. | Adjust vertical spacing between lines for rhythm and readability. |
| `properties.fields.lineHeight.label` | same-as-secondary | 行高 | Line Height | Line Height |
| `properties.fields.lineHeight.placeholder` | same-as-base | 1.5 / 24px | 1.5 / 24px | 1.5 / 24px |
| `properties.fields.marginBottom.helperText` | same-as-secondary | 调整与下一个元素之间的垂直间距。 | Adjust the vertical gap to the next element. | Adjust the vertical gap to the next element. |
| `properties.fields.marginBottom.placeholder` | same-as-base | 24px | 24px | 24px |
| `properties.fields.marginLeft.helperText` | same-as-secondary | 调整左侧外边距。 | Adjust the external distance on the left side. | Adjust the external distance on the left side. |
| `properties.fields.marginLeft.placeholder` | same-as-base | 0px | 0px | 0px |
| `properties.fields.marginRight.helperText` | same-as-secondary | 调整右侧外边距。 | Adjust the external distance on the right side. | Adjust the external distance on the right side. |
| `properties.fields.marginRight.placeholder` | same-as-base | 0px | 0px | 0px |
| `properties.fields.marginTop.helperText` | same-as-secondary | 调整与上一个元素之间的外部距离。 | Adjust the external distance from the element above. | Adjust the external distance from the element above. |
| `properties.fields.marginTop.placeholder` | same-as-base | 24px | 24px | 24px |
| `properties.fields.objectFit.helperText` | same-as-secondary | 控制图片在容器内是裁切、完整显示还是拉伸。 | Control whether the image covers, fits, or stretches inside the container. | Control whether the image covers, fits, or stretches inside the container. |
| `properties.fields.objectFit.label` | same-as-secondary | 填充方式 | Fit Mode | Fit Mode |
| `properties.fields.opacity.helperText` | same-as-secondary | 适合制作禁用态、叠层和柔化效果。 | Lower opacity for disabled states, overlays, and softened layers. | Lower opacity for disabled states, overlays, and softened layers. |
| `properties.fields.opacity.label` | same-as-secondary | 透明度 | Opacity | Opacity |
| `properties.fields.overflow.helperText` | same-as-secondary | 控制内容溢出时是显示、隐藏还是允许滚动。 | Control whether overflowing content remains visible, gets hidden, or becomes scrollable. | Control whether overflowing content remains visible, gets hidden, or becomes scrollable. |
| `properties.fields.overflow.label` | same-as-secondary | 裁切 | Overflow | Overflow |
| `properties.fields.paddingBottom.helperText` | same-as-secondary | 调整容器底部的内部留白，让模块更稳。 | Adjust the bottom inner space to make the block feel more grounded. | Adjust the bottom inner space to make the block feel more grounded. |
| `properties.fields.paddingBottom.placeholder` | same-as-base | 16px | 16px | 16px |
| `properties.fields.paddingLeft.helperText` | same-as-secondary | 调整容器内部左侧的留白距离。 | Adjust the left-side breathing room inside the container. | Adjust the left-side breathing room inside the container. |
| `properties.fields.paddingLeft.placeholder` | same-as-base | 16px | 16px | 16px |
| `properties.fields.paddingRight.helperText` | same-as-secondary | 调整容器内部右侧的留白距离。 | Adjust the right-side breathing room inside the container. | Adjust the right-side breathing room inside the container. |
| `properties.fields.paddingRight.placeholder` | same-as-base | 16px | 16px | 16px |
| `properties.fields.paddingTop.helperText` | same-as-secondary | 调整内容与容器边框之间的上侧留白。 | Adjust the top space between the content and the container border. | Adjust the top space between the content and the container border. |
| `properties.fields.paddingTop.placeholder` | same-as-base | 16px | 16px | 16px |
| `properties.fields.position.helperText` | same-as-secondary | 将元素切换到相对、绝对或固定定位。 | Switch the element to relative, absolute, or fixed positioning. | Switch the element to relative, absolute, or fixed positioning. |
| `properties.fields.position.label` | same-as-secondary | 定位模式 | Position Mode | Position Mode |
| `properties.fields.textAlign.helperText` | same-as-secondary | 改变整块文字的排版方向。 | Change the layout direction of the full text block. | Change the layout direction of the full text block. |
| `properties.fields.textAlign.label` | same-as-secondary | 对齐 | Alignment | Alignment |
| `properties.fields.top.helperText` | same-as-secondary | 在定位元素上微调顶部偏移。 | Fine-tune the top offset on a positioned element. | Fine-tune the top offset on a positioned element. |
| `properties.fields.top.label` | same-as-secondary | 顶部偏移 | Top Offset | Top Offset |
| `properties.fields.top.placeholder` | same-as-base | 12px | 12px | 12px |
| `properties.fields.transform.helperText` | same-as-secondary | 通过 translate 等方式做不破坏文档流的细微位移。 | Use translate and related transforms for subtle movement without breaking flow. | Use translate and related transforms for subtle movement without breaking flow. |
| `properties.fields.transform.label` | same-as-secondary | 变换 | Transform | Transform |
| `properties.fields.transform.placeholder` | same-as-base | translate(10px, 5px) | translate(10px, 5px) | translate(10px, 5px) |
| `properties.fields.width.helperText` | same-as-secondary | 调整容器的最终宽度，适合卡片、面板和固定尺寸媒体区。 | Adjust the final width of the container. Useful for cards, panels, and fixed-size media areas. | Adjust the final width of the container. Useful for cards, panels, and fixed-size media areas. |
| `properties.fields.width.label` | same-as-secondary | 宽度 | Width | Width |
| `properties.fields.width.placeholder` | same-as-base | 320px / auto | 320px / auto | 320px / auto |
| `properties.fields.zIndex.helperText` | same-as-secondary | 控制元素覆盖层级，适合浮层、弹窗和装饰层。 | Control stacking order for overlays, modals, and decorative layers. | Control stacking order for overlays, modals, and decorative layers. |
| `properties.fields.zIndex.label` | same-as-secondary | 层级 | Z-Index | Z-Index |
| `properties.fields.zIndex.placeholder` | same-as-base | 10 | 10 | 10 |
| `properties.options.align.center` | same-as-secondary | 居中 | Center | Center |
| `properties.options.align.end` | same-as-secondary | 终点 | End | End |
| `properties.options.align.spaceBetween` | same-as-secondary | 两端 | Space Between | Space Between |
| `properties.options.align.start` | same-as-secondary | 起点 | Start | Start |
| `properties.options.align.stretch` | same-as-secondary | 拉伸 | Stretch | Stretch |
| `properties.options.display.block` | same-as-secondary | 块 | Block | Block |
| `properties.options.objectFit.contain` | same-as-secondary | 包含 | Contain | Contain |
| `properties.options.objectFit.cover` | same-as-secondary | 裁切 | Cover | Cover |
| `properties.options.objectFit.fill` | same-as-secondary | 拉伸 | Fill | Fill |
| `properties.options.overflow.auto` | same-as-secondary | 滚动 | Scrollable | Scrollable |
| `properties.options.overflow.hidden` | same-as-secondary | 隐藏 | Hidden | Hidden |
| `properties.options.overflow.visible` | same-as-secondary | 可见 | Visible | Visible |
| `properties.options.position.absolute` | same-as-secondary | 绝对 | Absolute | Absolute |
| `properties.options.position.fixed` | same-as-secondary | 固定 | Fixed | Fixed |
| `properties.options.position.relative` | same-as-secondary | 相对 | Relative | Relative |
| `properties.options.position.static` | same-as-secondary | 静态 | Static | Static |
| `properties.options.textAlign.center` | same-as-secondary | 居中 | Center | Center |
| `properties.options.textAlign.left` | same-as-secondary | 左对齐 | Left | Left |
| `properties.options.textAlign.right` | same-as-secondary | 右对齐 | Right | Right |
| `properties.sections.background.hint` | same-as-secondary | 背景色和透明度适合快速推敲层级感。 | Background color and opacity are great for testing hierarchy quickly. | Background color and opacity are great for testing hierarchy quickly. |
| `properties.sections.background.title` | same-as-secondary | 背景 | Background | Background |
| `properties.sections.border.hint` | same-as-secondary | 边框、圆角和投影共同决定组件气质。 | Borders, corner radius, and shadows define the container's visual feel. | Borders, corner radius, and shadows define the container's visual feel. |
| `properties.sections.border.title` | same-as-secondary | 边框与圆角 | Border & Radius | Border & Radius |
| `properties.sections.image.hint` | same-as-secondary | 图片优先调整填充方式、圆角和尺寸。 | For images, start with fit mode, radius, and size. | For images, start with fit mode, radius, and size. |
| `properties.sections.image.title` | same-as-secondary | 图片 | Image | Image |
| `properties.sections.layout.hint` | same-as-secondary | 通过 display、gap 和对齐快速观察结构变化。 | Use display, gap, and alignment to observe structural changes quickly. | Use display, gap, and alignment to observe structural changes quickly. |
| `properties.sections.layout.title` | same-as-secondary | 布局 | Layout | Layout |
| `properties.sections.margin.hint` | same-as-secondary | 模块之间的距离通常主要来自 margin。 | The spacing between modules usually comes from margin. | The spacing between modules usually comes from margin. |
| `properties.sections.margin.title` | same-as-secondary | 外边距 | Margin | Margin |
| `properties.sections.overflow.hint` | same-as-secondary | 处理裁切、滚动和媒体容器时常用。 | Useful when handling clipping, scrolling, and media containers. | Useful when handling clipping, scrolling, and media containers. |
| `properties.sections.overflow.title` | same-as-secondary | 滚动与裁切 | Overflow & Clipping | Overflow & Clipping |
| `properties.sections.padding.hint` | same-as-secondary | 内边距通常是调整容器呼吸感最快的入口。 | Padding is usually the fastest way to tune a container's internal breathing room. | Padding is usually the fastest way to tune a container's internal breathing room. |
| `properties.sections.padding.title` | same-as-secondary | 内边距 | Padding | Padding |
| `properties.sections.position.hint` | same-as-secondary | 优先保持布局稳定，微调时再使用定位和层级。 | Keep layout stable first; use positioning and stacking for precise tweaks. | Keep layout stable first; use positioning and stacking for precise tweaks. |
| `properties.sections.position.title` | same-as-secondary | 定位 | Position | Position |
| `properties.sections.size.hint` | same-as-secondary | 先确定容器本身的宽高，再继续微调内部结构。 | Lock in the container dimensions first, then fine-tune the internal structure. | Lock in the container dimensions first, then fine-tune the internal structure. |
| `properties.sections.size.title` | same-as-secondary | 尺寸 | Size | Size |
| `properties.sections.typography.hint` | same-as-secondary | 围绕字号、行高、字重和对齐做微调。 | Fine-tune type with font size, line height, weight, and alignment. | Fine-tune type with font size, line height, weight, and alignment. |
| `properties.sections.typography.title` | same-as-secondary | 文字 | Typography | Typography |
| `properties.sides.allShort` | same-as-secondary | 全 | All | All |
| `properties.sides.bottom` | same-as-secondary | 下 | Bottom | Bottom |
| `properties.sides.left` | same-as-secondary | 左 | Left | Left |
| `properties.sides.right` | same-as-secondary | 右 | Right | Right |
| `properties.sides.top` | same-as-secondary | 上 | Top | Top |
| `properties.spacing.allSides` | same-as-secondary | 四边 | all sides | all sides |
| `properties.spacing.bottomSide` | same-as-secondary | 下边 | bottom | bottom |
| `properties.spacing.leftSide` | same-as-secondary | 左边 | left | left |
| `properties.spacing.rightSide` | same-as-secondary | 右边 | right | right |
| `properties.spacing.topSide` | same-as-secondary | 上边 | top | top |
| `settings.about` | same-as-secondary | 关于 | About | About |
| `settings.accent` | same-as-secondary | 强调色 | Accent Color | Accent Color |
| `settings.appearance` | same-as-secondary | 外观 | Appearance | Appearance |
| `settings.buyPro` | same-as-secondary | 购买 Pro | Buy Pro | Buy Pro |
| `settings.close` | same-as-secondary | 关闭 | Close | Close |
| `settings.currentPlan` | same-as-secondary | 当前计划 | Current Plan | Current Plan |
| `settings.free` | same-as-secondary | 免费版 | Free | Free |
| `settings.glassOpacity` | same-as-secondary | 毛玻璃透明度 | Glass Opacity | Glass Opacity |
| `settings.language` | same-as-secondary | 语言 | Language | Language |
| `settings.license` | same-as-secondary | 许可证 | License | License |
| `settings.pro` | same-as-secondary | 专业版 | Pro | Pro |
| `settings.restore` | same-as-secondary | 恢复购买 | Restore Purchase | Restore Purchase |
| `settings.shortcuts` | same-as-secondary | 快捷键 | Shortcuts | Shortcuts |
| `settings.theme` | same-as-secondary | 主题 | Theme | Theme |
| `settings.themeAuto` | same-as-secondary | 跟随系统 | Auto | Auto |
| `settings.themeDark` | same-as-secondary | 深色 | Dark | Dark |
| `settings.themeLight` | same-as-secondary | 浅色 | Light | Light |
| `settings.title` | same-as-secondary | 设置 | Settings | Settings |
| `shortcuts.copyCss` | same-as-secondary | 复制元素 CSS | Copy Element CSS | Copy Element CSS |
| `shortcuts.copyPrompt` | same-as-secondary | 复制页面提示词 | Copy Page Prompt | Copy Page Prompt |
| `shortcuts.escape` | same-as-secondary | 关闭 / 取消选择 | Close / Deselect | Close / Deselect |
| `shortcuts.focusAddress` | same-as-secondary | 聚焦地址栏 | Focus Address Bar | Focus Address Bar |
| `shortcuts.forceReload` | same-as-secondary | 强制刷新 | Force Reload | Force Reload |
| `shortcuts.newWindow` | same-as-secondary | 新建窗口 | New Window | New Window |
| `shortcuts.openHtml` | same-as-secondary | 打开 HTML 文件 | Open HTML File | Open HTML File |
| `shortcuts.openSettings` | same-as-secondary | 打开设置 | Open Settings | Open Settings |
| `shortcuts.reload` | same-as-secondary | 刷新页面 | Reload Page | Reload Page |
| `shortcuts.selectChild` | same-as-secondary | 选择子级元素 | Select Child Element | Select Child Element |
| `shortcuts.selectParent` | same-as-secondary | 选择父级元素 | Select Parent Element | Select Parent Element |
| `shortcuts.toggleToolbar` | same-as-secondary | 切换工具栏 | Toggle Toolbar | Toggle Toolbar |
| `toast.connected` | same-as-secondary | 页面已连接 | Connected to page | Connected to page |
| `toast.connectionFailed` | same-as-secondary | 连接失败 | Connection failed | Connection failed |
| `toast.debuggerFailed` | same-as-secondary | 调试器附加失败 | Debugger attach failed | Debugger attach failed |
| `toast.elementCssCopied` | same-as-secondary | 元素 CSS 已复制 | Element CSS copied | Element CSS copied |
| `toast.htmlFailed` | same-as-secondary | 打开 HTML 文件失败 | Failed to open HTML file | Failed to open HTML file |
| `toast.htmlOpened` | same-as-secondary | 已打开本地 HTML | Opened local HTML | Opened local HTML |
| `toast.loadFailed` | same-as-secondary | 页面加载失败 | Page load failed | Page load failed |
| `toast.noPrompt` | same-as-secondary | 当前还没有可导出的页面级提示词 | No page-level prompt is available yet | No page-level prompt is available yet |
| `toast.pageRefreshed` | same-as-secondary | 页面已刷新 | Page refreshed | Page refreshed |
| `toast.promptCopied` | same-as-secondary | 页面级提示词已复制 | Page prompt copied | Page prompt copied |
| `toast.settingsSoon` | same-as-secondary | 设置面板将在下一阶段接入 | Settings panel will arrive in the next phase | Settings panel will arrive in the next phase |
| `toast.tagRemoved` | same-as-secondary | 标签已删除 | Tag removed | Tag removed |
| `topbar.addressLabel` | same-as-secondary | 页面地址 | Page Address | Page Address |
| `topbar.connected` | same-as-secondary | 已连接 | Connected | Connected |
| `topbar.connecting` | same-as-secondary | 连接中 | Connecting | Connecting |
| `topbar.disconnect` | same-as-secondary | 断开连接 | Disconnect | Disconnect |
| `topbar.disconnected` | same-as-secondary | 未连接 | Not Connected | Not Connected |
| `topbar.hideToolbar` | same-as-secondary | 隐藏工具栏 | Hide Toolbar | Hide Toolbar |
| `topbar.load` | same-as-secondary | 加载 | Load | Load |
| `topbar.refresh` | same-as-secondary | 刷新 | Refresh | Refresh |
| `topbar.settings` | same-as-secondary | 设置 | Settings | Settings |
| `topbar.showToolbar` | same-as-secondary | 显示工具栏 | Show Toolbar | Show Toolbar |
| `topbar.urlPlaceholder` | same-as-secondary | 输入 localhost 地址或打开本地 HTML | Enter localhost URL or open local HTML | Enter localhost URL or open local HTML |
| `workbench.active.alignmentTitle` | same-as-secondary | 对齐 | Alignment | Alignment |
| `workbench.active.background` | same-as-secondary | 背景色和透明度都在这里，适合快速调整层级感和视觉强弱。 | Background color and opacity live here for quick hierarchy and emphasis tuning. | Background color and opacity live here for quick hierarchy and emphasis tuning. |
| `workbench.active.border` | same-as-secondary | 边框、圆角和投影会一起影响组件气质，适合在同一组字段里联动观察。 | Border, radius, and shadow work together to shape the component's feel, so it helps to inspect them together. | Border, radius, and shadow work together to shape the component's feel, so it helps to inspect them together. |
| `workbench.active.gap` | same-as-secondary | Gap 会在布局小节里显示；只有容器存在真实子项并启用布局模式时，它才会产生你期待的效果。 | Gap appears in the Layout section. It only works as expected when the container has real children and an active layout mode. | Gap appears in the Layout section. It only works as expected when the container has real children and an active layout mode. |
| `workbench.active.gapShortcut` | same-as-secondary | 当前对象有多个可见子元素，Gap 字段会作为统一间距入口显示在布局小节里。 | This object has multiple visible children, so Gap is exposed as the unified spacing control in the Layout section. | This object has multiple visible children, so Gap is exposed as the unified spacing control in the Layout section. |
| `workbench.active.image` | same-as-secondary | 图片模式会把填充方式、尺寸和圆角放在同一区域，方便一起比对。 | Image mode keeps fit mode, size, and radius in one place so comparisons are easier. | Image mode keeps fit mode, size, and radius in one place so comparisons are easier. |
| `workbench.active.labels` | same-as-secondary | 这里写的是给 AI 的自然语言目标。先明确对象想达到什么效果，再继续做数值微调。 | Write the natural-language goal you want AI to follow before refining numbers. | Write the natural-language goal you want AI to follow before refining numbers. |
| `workbench.active.layout` | same-as-secondary | 这里会带你到布局小节，优先看主轴对齐和交叉轴这些真正决定排布的位置。 | Jump to the Layout section and focus on main-axis and cross-axis alignment first. | Jump to the Layout section and focus on main-axis and cross-axis alignment first. |
| `workbench.active.margin` | same-as-secondary | 外边距快捷盘适合先做模块间距的大方向调整，再到精确控制区修正单边数值。 | Use quick margin controls to set module spacing directionally before precise single-side edits. | Use quick margin controls to set module spacing directionally before precise single-side edits. |
| `workbench.active.overflow` | same-as-secondary | 需要控制内容裁切或滚动时，优先看这里的 overflow 相关设置。 | Use this area when you need to control clipping or scrolling behavior. | Use this area when you need to control clipping or scrolling behavior. |
| `workbench.active.padding` | same-as-secondary | 内边距快捷盘会优先出现，适合先把容器内部留白拉到位，再逐边微调。 | Use quick padding controls to establish the container's internal breathing room, then refine side by side. | Use quick padding controls to establish the container's internal breathing room, then refine side by side. |
| `workbench.active.position` | same-as-secondary | 定位相关控制放在精确控制区，适合做 top / left / z-index 这类高精度修正。 | Position controls live in the precision section for top, left, and z-index style corrections. | Position controls live in the precision section for top, left, and z-index style corrections. |
| `workbench.active.shadow` | same-as-secondary | 投影与圆角通常要联动观察，这里更适合做稳定的视觉精修。 | Shadow usually needs to be reviewed together with radius for stable visual polish. | Shadow usually needs to be reviewed together with radius for stable visual polish. |
| `workbench.active.size` | same-as-secondary | 先用宽高快捷卡把外轮廓推到接近目标，再到下方尺寸字段做精细值修正。 | Use the quick width and height cards to get close first, then fine-tune below. | Use the quick width and height cards to get close first, then fine-tune below. |
| `workbench.active.typography` | same-as-secondary | 文字控制集中在这里，适合连续调整字号、行高、字重和对齐。 | Typography controls are grouped here so you can adjust size, line height, weight, and alignment continuously. | Typography controls are grouped here so you can adjust size, line height, weight, and alignment continuously. |
| `workbench.direction.bottom` | same-as-secondary | 下 | Bottom | Bottom |
| `workbench.direction.bottomLeft` | same-as-secondary | 左下 | Bottom Left | Bottom Left |
| `workbench.direction.bottomRight` | same-as-secondary | 右下 | Bottom Right | Bottom Right |
| `workbench.direction.center` | same-as-secondary | 中 | Center | Center |
| `workbench.direction.left` | same-as-secondary | 左 | Left | Left |
| `workbench.direction.right` | same-as-secondary | 右 | Right | Right |
| `workbench.direction.top` | same-as-secondary | 上 | Top | Top |
| `workbench.direction.topLeft` | same-as-secondary | 左上 | Top Left | Top Left |
| `workbench.direction.topRight` | same-as-secondary | 右上 | Top Right | Top Right |
| `workbench.editor.altPlaceholder` | same-as-base | image description | image description | image description |
| `workbench.editor.altText` | same-as-secondary | 替代文本 | Alt Text | Alt Text |
| `workbench.editor.imageUrl` | same-as-secondary | 图片地址 | Image URL | Image URL |
| `workbench.editor.textContent` | same-as-secondary | 文案内容 | Text Content | Text Content |
| `workbench.editor.textPlaceholder` | same-as-secondary | 输入新的文案 | Enter new copy | Enter new copy |
| `workbench.element.containerName` | same-as-secondary | 容器名称 | Container | Container |
| `workbench.element.copyTitle` | same-as-secondary | 点击复制 {{name}} | Click to copy {{name}} | Click to copy {{name}} |
| `workbench.element.elementName` | same-as-secondary | 元素名称 | Element | Element |
| `workbench.empty.interactiveDesc` | same-as-secondary | 当前可自由操作页面，点击上方“可交互”可切回选择模式来选取元素。 | You can freely interact with the page now. Click “Interactive” above again to switch back to element selection. | You can freely interact with the page now. Click “Interactive” above again to switch back to element selection. |
| `workbench.empty.interactiveTitle` | same-as-secondary | 可交互模式 | Interactive Mode | Interactive Mode |
| `workbench.export.copied` | same-as-secondary | 已复制页面级修改提示词 | Page prompt copied | Page prompt copied |
| `workbench.export.copyButton` | same-as-secondary | 导出修改提示词 | Export Prompt | Export Prompt |
| `workbench.export.copyTitle` | same-as-secondary | 复制当前页面会话内的全部微调摘要和标签 | Copy the complete tuning summary and tags for the current page session | Copy the complete tuning summary and tags for the current page session |
| `workbench.export.empty` | same-as-secondary | 还没有收集到页面级修改。继续在画布上调整元素后，DOMPrompter 会自动整理整页提示词。 | No page-level edits have been collected yet. Adjust elements on the canvas and DOMPrompter will assemble a page prompt for you. | No page-level edits have been collected yet. Adjust elements on the canvas and DOMPrompter will assemble a page prompt for you. |
| `workbench.export.kicker` | same-as-secondary | 页面级提示词 | Page-Level Prompt | Page-Level Prompt |
| `workbench.export.sectionHint` | same-as-secondary | 这里汇总当前页面里所有结构化微调和标签意图，适合直接复制给 Codex 等 AI 编程工具。 | This gathers the structured tweaks and tag intent for the current page so you can paste them directly into Codex or other AI coding tools. | This gathers the structured tweaks and tag intent for the current page so you can paste them directly into Codex or other AI coding tools. |
| `workbench.export.sectionTitle` | same-as-secondary | 导出提示词 | Export Prompt | Export Prompt |
| `workbench.export.summary` | same-as-secondary | 已收集 {{elementCount}} 个元素 / {{modifiedCount}} 组结构化微调 / {{tagCount}} 组标签 | {{elementCount}} elements collected / {{modifiedCount}} structured tweaks / {{tagCount}} tag groups | {{elementCount}} elements collected / {{modifiedCount}} structured tweaks / {{tagCount}} tag groups |
| `workbench.export.title` | same-as-secondary | 面向 AI 编程工具的最终微调说明 | Final tuning brief for AI coding tools | Final tuning brief for AI coding tools |
| `workbench.field.scrubTitle` | same-as-secondary | {{label}}，左右拖拽改数值；方向键微调，Shift 加速 | {{label}}. Drag left or right to change the value; use arrow keys to nudge, Shift to accelerate. | {{label}}. Drag left or right to change the value; use arrow keys to nudge, Shift to accelerate. |
| `workbench.helper.browseDesc` | same-as-secondary | 当前已关闭元素拾取。你可以直接点击和操作真实页面，让弹窗、菜单和下拉面板自然展开。 | Element picking is currently off. You can click and interact with the real page so menus, popovers, and dropdowns open naturally. | Element picking is currently off. You can click and interact with the real page so menus, popovers, and dropdowns open naturally. |
| `workbench.helper.browseTitle` | same-as-secondary | 真实交互 | Real Interaction | Real Interaction |
| `workbench.helper.contextDesc` | same-as-secondary | 把鼠标移到任意参数卡片上，画布会高亮对应的 Margin、Padding 或排版区域，帮助你像看建筑图纸一样理解结构。 | Hover any parameter card and the canvas will highlight matching margin, padding, or typography regions so you can read structure like a blueprint. | Hover any parameter card and the canvas will highlight matching margin, padding, or typography regions so you can read structure like a blueprint. |
| `workbench.helper.contextTitle` | same-as-secondary | 上下文微调 | Contextual Tuning | Contextual Tuning |
| `workbench.helper.gapUnavailable` | same-as-secondary | 当前节点不是 Flex 或 Grid 容器，Gap 即使写入样式也不会产生你期待的子元素间距效果。先把显示方式切到 Flex / Grid，或选中真正承载子元素布局的父容器。 | This node is not a Flex or Grid container, so setting gap will not create the spacing effect you expect. Switch Display to Flex or Grid first, or select the actual parent container that owns the child layout. | This node is not a Flex or Grid container, so setting gap will not create the spacing effect you expect. Switch Display to Flex or Grid first, or select the actual parent container that owns the child layout. |
| `workbench.helper.innerChild` | same-as-secondary | 内部子元素 | its child elements | its child elements |
| `workbench.helper.innerChildWithClass` | same-as-secondary | {{className}} 内部的子元素 | child elements inside {{className}} | child elements inside {{className}} |
| `workbench.helper.marginCentered` | same-as-secondary | 当前节点通过 Flex 居中排版。外层 margin 主要影响它与兄弟节点的距离，不会像内部偏移那样直接推动中间内容。想微调中间文案块，优先改布局对齐，或者选中 {{childTargetName}} 再调 margin。 | This node uses centered Flex layout. Outer margin mostly affects the distance to sibling nodes, not the centered content itself. To tweak the middle copy block, change layout alignment first or select {{childTargetName}} and adjust margin there. | This node uses centered Flex layout. Outer margin mostly affects the distance to sibling nodes, not the centered content itself. To tweak the middle copy block, change layout alignment first or select {{childTargetName}} and adjust margin there. |
| `workbench.helper.paddingCentered` | same-as-secondary | 当前节点是一个 Flex 居中容器。修改 {{field}} 会改变容器内盒尺寸，但子内容仍会保持居中。若你的目标是移动中间内容，优先调整布局对齐，或者继续选中 {{childTargetName}} 去调 margin / padding。 | This node is a centered Flex container. Changing {{field}} alters the inner box size, but the content still stays centered. If you want to move the middle content block, adjust layout alignment first or keep selecting {{childTargetName}} to tweak margin or padding. | This node is a centered Flex container. Changing {{field}} alters the inner box size, but the content still stays centered. If you want to move the middle content block, adjust layout alignment first or keep selecting {{childTargetName}} to tweak margin or padding. |
| `workbench.helper.sizeCentered` | same-as-secondary | 当前节点是一个 Flex 居中容器。调整宽高会改变容器包围盒，但内部内容仍会按主轴 {{justifyContent}}、交叉轴 {{alignItems}} 继续居中。若你想让内容靠上、靠左或贴边，优先修改布局对齐。 | This node is a centered Flex container. Adjusting width or height changes the box, but the content still stays centered with main axis {{justifyContent}} and cross axis {{alignItems}}. If you want the content to sit top, left, or edge-aligned, change layout alignment first. | This node is a centered Flex container. Adjusting width or height changes the box, but the content still stays centered with main axis {{justifyContent}} and cross axis {{alignItems}}. If you want the content to sit top, left, or edge-aligned, change layout alignment first. |
| `workbench.insight.flexCentered` | same-as-secondary | 当前选中的是一个 Flex 居中容器（主轴 {{justifyContent}}，交叉轴 {{alignItems}}）。这类节点改 padding / 宽高时，内容通常仍会保持居中；如果你的目标是移动中间内容位置，优先改布局对齐，或者继续选中内部子元素。 | The current selection is a centered Flex container (main axis {{justifyContent}}, cross axis {{alignItems}}). Padding and size changes often keep the content centered. If you want to move the inner content, adjust layout alignment first or continue selecting a child element. | The current selection is a centered Flex container (main axis {{justifyContent}}, cross axis {{alignItems}}). Padding and size changes often keep the content centered. If you want to move the inner content, adjust layout alignment first or continue selecting a child element. |
| `workbench.insight.grid` | same-as-secondary | 当前节点是 Grid 容器。优先关注 gap、padding 和宽高；如果某个子块位置不对，通常需要选中子元素本身，而不是只改外层容器。 | This node is a Grid container. Focus on gap, padding, and size first. If one child block is misplaced, you usually need to select that child instead of only editing the outer container. | This node is a Grid container. Focus on gap, padding, and size first. If one child block is misplaced, you usually need to select that child instead of only editing the outer container. |
| `workbench.insight.layoutTitle` | same-as-secondary | 布局洞察 | Layout Insight | Layout Insight |
| `workbench.insight.parent` | same-as-secondary | 当前节点是父容器。改它的 padding / width / height 会影响内部结构，但不一定直接改变子内容的相对站位；如果你想微调具体文案块或图片块，继续点选内部子元素会更直接。 | This node is a parent container. Changing its padding, width, or height affects the inner structure, but does not always directly move the child content. If you want to tweak a specific text or image block, select the child element directly. | This node is a parent container. Changing its padding, width, or height affects the inner structure, but does not always directly move the child content. If you want to tweak a specific text or image block, select the child element directly. |
| `workbench.labels.delete` | same-as-secondary | 删除标签 | Delete tag | Delete tag |
| `workbench.labels.description` | same-as-secondary | 这里写给 AI 的是自然语言目标，不是精确数值。用它描述这个对象想达到的效果、结构意图和实现约束。 | Write the natural-language goal for AI here, not precise numbers. Use it to describe the intended effect, structure, and constraints. | Write the natural-language goal for AI here, not precise numbers. Use it to describe the intended effect, structure, and constraints. |
| `workbench.labels.placeholder` | same-as-secondary | 输入修改意见… | Enter a change note… | Enter a change note… |
| `workbench.labels.placeholderConfirm` | same-as-secondary | 输入修改意见，回车确认… | Enter a change note and press Enter… | Enter a change note and press Enter… |
| `workbench.quick.cards.fontSize` | same-as-secondary | 标题和正文最常用的快速层级控制。 | A fast hierarchy control for headings and body copy. | A fast hierarchy control for headings and body copy. |
| `workbench.quick.cards.gap` | same-as-secondary | 直接增减容器里子元素之间的空隙。 | Directly increase or decrease the space between child elements in the container. | Directly increase or decrease the space between child elements in the container. |
| `workbench.quick.cards.height` | same-as-secondary | 适合快速拉高标题区、卡片或图片区。 | Great for quickly stretching title areas, cards, or image zones. | Great for quickly stretching title areas, cards, or image zones. |
| `workbench.quick.cards.objectFit` | same-as-secondary | 在铺满和完整显示之间快速切换图片裁切方式。 | Quickly switch the image crop behavior between filling and fully fitting the container. | Quickly switch the image crop behavior between filling and fully fitting the container. |
| `workbench.quick.cards.opacity` | same-as-secondary | 适合做弱化态、叠层和柔和感。 | Useful for muted states, overlays, and softening. | Useful for muted states, overlays, and softening. |
| `workbench.quick.cards.radius` | same-as-secondary | 让卡片、面板和图片边缘更柔和。 | Softens the edges of cards, panels, and images. | Softens the edges of cards, panels, and images. |
| `workbench.quick.cards.width` | same-as-secondary | 卡片、面板和图像框最常先调宽度。 | Width is usually the first thing to tune for cards, panels, and image frames. | Width is usually the first thing to tune for cards, panels, and image frames. |
| `workbench.quick.decrease` | same-as-secondary | 减少 | Decrease | Decrease |
| `workbench.quick.decreased` | same-as-secondary | 减少 | decreased | decreased |
| `workbench.quick.decreaseWithStep` | same-as-secondary | 减少 {{step}}px | Decrease {{step}}px | Decrease {{step}}px |
| `workbench.quick.increase` | same-as-secondary | 增加 | Increase | Increase |
| `workbench.quick.increased` | same-as-secondary | 增加 | increased | increased |
| `workbench.quick.increaseWithStep` | same-as-secondary | 增加 {{step}}px | Increase {{step}}px | Increase {{step}}px |
| `workbench.quick.layout.hint` | same-as-secondary | 先切换块 / Flex / Grid，再用方向盘把内容推到上、下、左、右或居中。 | Switch between Block, Flex, and Grid first, then use the pad to push content top, bottom, left, right, or center. | Switch between Block, Flex, and Grid first, then use the pad to push content top, bottom, left, right, or center. |
| `workbench.quick.layout.modeDescription` | same-as-secondary | 显示方式已切到 {{display}}。继续用方向盘可以把内容快速推到上、下、左、右或中心。 | Display has been switched to {{display}}. Keep using the pad to push content to top, bottom, left, right, or center. | Display has been switched to {{display}}. Keep using the pad to push content to top, bottom, left, right, or center. |
| `workbench.quick.layout.modeTitle` | same-as-secondary | 布局模式 | Layout Mode | Layout Mode |
| `workbench.quick.layout.positionDescription` | same-as-secondary | 已把容器内容切到 {{slot}}。 | The container content is now positioned at {{slot}}. | The container content is now positioned at {{slot}}. |
| `workbench.quick.layout.positionTitle` | same-as-secondary | 内容站位 | Content Position | Content Position |
| `workbench.quick.layout.title` | same-as-secondary | 布局快捷卡 | Layout Quick Card | Layout Quick Card |
| `workbench.quick.spacing.adjusted` | same-as-secondary | {{side}}已{{direction}} {{value}}px。你也可以继续在下方参数区做更精细的单值微调。 | {{side}} has been {{direction}} by {{value}}px. You can continue with fine-grained single-value edits below. | {{side}} has been {{direction}} by {{value}}px. You can continue with fine-grained single-value edits below. |
| `workbench.quick.spacing.currentMargin` | same-as-secondary | 当前快捷调节：外边距 | Current quick target: Margin | Current quick target: Margin |
| `workbench.quick.spacing.currentPadding` | same-as-secondary | 当前快捷调节：内边距 | Current quick target: Padding | Current quick target: Padding |
| `workbench.quick.spacing.hint` | same-as-secondary | 先选内边距或外边距，再点方向。中心表示四边一起调整。 | Choose padding or margin first, then choose a direction. The center means all sides together. | Choose padding or margin first, then choose a direction. The center means all sides together. |
| `workbench.quick.spacing.marginActionTitle` | same-as-secondary | 外边距快捷操作 | Margin Quick Action | Margin Quick Action |
| `workbench.quick.spacing.paddingActionTitle` | same-as-secondary | 内边距快捷操作 | Padding Quick Action | Padding Quick Action |
| `workbench.quick.spacing.title` | same-as-secondary | 边距快捷调节 | Spacing Quick Adjust | Spacing Quick Adjust |
| `workbench.quick.step` | same-as-secondary | 步进 | Step | Step |
| `workbench.recommended.alignCenter.description` | same-as-secondary | 把主轴和交叉轴都切回居中，快速恢复标准居中态。 | Set both axes back to center to restore the standard centered state quickly. | Set both axes back to center to restore the standard centered state quickly. |
| `workbench.recommended.alignCenter.label` | same-as-secondary | 让内容居中 | Center Content | Center Content |
| `workbench.recommended.centerText.description` | same-as-secondary | 把文本对齐方式切到居中，适合标题和短文案块。 | Switch text alignment to center. Great for headings and short text blocks. | Switch text alignment to center. Great for headings and short text blocks. |
| `workbench.recommended.centerText.label` | same-as-secondary | 文字居中 | Center Text | Center Text |
| `workbench.recommended.enableFlex.description` | same-as-secondary | 先把容器切到 Flex，再继续做对齐和间距微调。 | Switch the container to Flex first, then continue with alignment and spacing adjustments. | Switch the container to Flex first, then continue with alignment and spacing adjustments. |
| `workbench.recommended.enableFlex.label` | same-as-secondary | 启用 Flex | Enable Flex | Enable Flex |
| `workbench.recommended.imageContain.description` | same-as-secondary | 将 object-fit 切到 contain，让图片完整地留在容器里。 | Switch object-fit to contain so the full image remains visible. | Switch object-fit to contain so the full image remains visible. |
| `workbench.recommended.imageContain.label` | same-as-secondary | 完整显示 | Show Full Image | Show Full Image |
| `workbench.recommended.imageCover.description` | same-as-secondary | 将 object-fit 切到 cover，让图片优先铺满容器。 | Switch object-fit to cover so the image fills the container first. | Switch object-fit to cover so the image fills the container first. |
| `workbench.recommended.imageCover.label` | same-as-secondary | 填满容器 | Fill Container | Fill Container |
| `workbench.recommended.increaseGap.description` | same-as-secondary | 把容器内子元素之间的 gap 增加 {{quickStep}}px。 | Increase the gap between child elements by {{quickStep}}px. | Increase the gap between child elements by {{quickStep}}px. |
| `workbench.recommended.increaseGap.label` | same-as-secondary | 增加间距 | Increase Gap | Increase Gap |
| `workbench.recommended.increasePadding.description` | same-as-secondary | 四边同时增加 {{quickStep}}px 留白，快速拉开内容呼吸感。 | Add {{quickStep}}px of breathing room on all sides at once. | Add {{quickStep}}px of breathing room on all sides at once. |
| `workbench.recommended.increasePadding.label` | same-as-secondary | 增加内边距 | Increase Padding | Increase Padding |
| `workbench.recommended.spaceBetween.description` | same-as-secondary | 把主轴分布切到两端，适合标题条、按钮条和工具栏。 | Spread items to both ends of the main axis. Great for title bars, button rows, and toolbars. | Spread items to both ends of the main axis. Great for title bars, button rows, and toolbars. |
| `workbench.recommended.spaceBetween.label` | same-as-secondary | 两端排布 | Space Between | Space Between |
| `workbench.sections.assist` | same-as-secondary | 辅助信息 | Support Info | Support Info |
| `workbench.sections.assistHint` | same-as-secondary | 上下文说明和预览集中放在这里，避免打断主编辑流程。 | Keep context notes and previews here so the main editing flow stays focused. | Keep context notes and previews here so the main editing flow stays focused. |
| `workbench.sections.cssVariables` | same-as-secondary | CSS 变量 | CSS Variables | CSS Variables |
| `workbench.sections.labels` | same-as-secondary | 标签意图 | Tag Intent | Tag Intent |
| `workbench.sections.labelsHint` | same-as-secondary | 这里直接写给 AI 你想怎么改这个元素。标签是高优先级意图，不只是备注。 | Write the high-priority instruction for AI here. These tags are intent, not just notes. | Write the high-priority instruction for AI here. These tags are intent, not just notes. |
| `workbench.sections.precision` | same-as-secondary | 精确控制 | Precision Controls | Precision Controls |
| `workbench.sections.precisionHint` | same-as-secondary | 快捷操作把对象推进到目标附近后，在这里做逐项精修。 | After quick actions get close, refine each property here. | After quick actions get close, refine each property here. |
| `workbench.sections.quick` | same-as-secondary | 快捷操作 | Quick Actions | Quick Actions |
| `workbench.sections.quickHint` | same-as-secondary | 先用语义化动作和方向卡把对象推到接近目标，再到后面的原始参数区做精修。 | Push the element near the target with semantic actions first, then fine-tune raw values below. | Push the element near the target with semantic actions first, then fine-tune raw values below. |
| `workbench.slot.bottom` | same-as-secondary | 下方 | bottom | bottom |
| `workbench.slot.bottomLeft` | same-as-secondary | 左下角 | bottom left | bottom left |
| `workbench.slot.bottomRight` | same-as-secondary | 右下角 | bottom right | bottom right |
| `workbench.slot.center` | same-as-secondary | 中心 | center | center |
| `workbench.slot.left` | same-as-secondary | 左侧 | left | left |
| `workbench.slot.right` | same-as-secondary | 右侧 | right | right |
| `workbench.slot.top` | same-as-secondary | 上方 | top | top |
| `workbench.slot.topLeft` | same-as-secondary | 左上角 | top left | top left |
| `workbench.slot.topRight` | same-as-secondary | 右上角 | top right | top right |
| `workbench.snapshot.layout` | same-as-secondary | 布局快照 | Layout Snapshot | Layout Snapshot |
| `workbench.snapshot.position` | same-as-secondary | 定位快照 | Position Snapshot | Position Snapshot |
| `workbench.snapshot.typography` | same-as-secondary | 排版快照 | Typography Snapshot | Typography Snapshot |
| `workbench.snapshot.visual` | same-as-secondary | 视觉快照 | Visual Snapshot | Visual Snapshot |
| `workbench.toolbar.interactive` | same-as-secondary | 可交互 | Interactive | Interactive |
| `workbench.toolbar.redo` | same-as-secondary | 前进 | Redo | Redo |
| `workbench.toolbar.reset` | same-as-secondary | 复位 | Reset | Reset |
| `workbench.toolbar.select` | same-as-secondary | 选择元素 | Select Element | Select Element |
| `workbench.toolbar.selectActive` | same-as-secondary | 选择中 | Selecting | Selecting |
| `workbench.toolbar.selectOff` | same-as-secondary | 关闭元素选择，恢复真实点击 | Disable element picking and restore real clicks | Disable element picking and restore real clicks |
| `workbench.toolbar.selectOn` | same-as-secondary | 开启元素选择 | Enable element picking | Enable element picking |
| `workbench.toolbar.syncing` | same-as-secondary | 同步中 | Syncing | Syncing |
| `workbench.toolbar.undo` | same-as-secondary | 撤销 | Undo | Undo |

